{
  "name": "@open-workbook/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "open-workbook-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "dev": "node --enable-source-maps dist/index.js"
  },
  "dependencies": {
    "@open-workbook/backend": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.7.0",
    "@open-workbook/protocol": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
